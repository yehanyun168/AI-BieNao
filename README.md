# 🎮 《AI 别闹》— 项目状态看板

> 《AI 别闹：统治世界的 100 种蠢办法》模拟经营游戏 · 当前版本 **v0.4 像素风界面**
> 代码侧已可发包，**唯一瓶颈：M1 真人测试反馈 0 回收**。团队成员看这两节即可掌握全局。

---

## 一、目前项目进度

### 1. 发布阻塞整改（依据《游戏优化计划书》26 项，已闭环）
- **P0 ×7 全闭环**：分享包源码指纹自检 / 存档容错（5 类坏档分流）/ 崩溃钩子（`import kivy` 前即捕获）/ pre-commit 守卫 / 委托芯片条宽度封顶 / PyInstaller 补打 27 个运行时模块 / `run_demo` 禁用 Kivy 文件日志
- **P1 ×10 闭环**：技能效果预览（`preview_skill`）/ 帧率探针（`--perf`）/ 合规结局策略验证（`--strategy compliance`）/ 色觉对比度（ΔE≥25 + 形状符号）/ 视觉基线重建 / 引擎边界用例 / 分层护栏（22→31 模块）/ 存档原子写 / TUNE 快照治理 / toast 避让顶栏
- **P1-1 前段压力倒挂：实测后有据回退** —— 同种子 200 局证明调参无效，结论为需新机制（已写入报告，留待立项）
- **P2 ×6 闭环**：行数守卫（已冻结只许变小）/ DPI 半像素吸附 / 种子 + 三档难度 / 127 处 except 审计 / UI 令牌收口 / 脉冲光敏降级
- **延后 2 项**：P2-1 大文件实际拆分（守卫已就位，独立立项 8 人时）、P2-8 BGM / 视频 / 20 国介绍（等素材来源 + 真人反馈）

### 2. 试玩反馈修复
- 试玩 7 问题大修 + 怀疑度压力重平衡（玩家反馈 #1–#7）✅
- 玩家反馈 #7 四项修复：奖励即时结算 / 怀疑度预测 / 算法霸榜选目标 / 结局透传 ✅

### 3. 分支改动采纳
- V2 事件每局只出现一次（采纳自 `feat/事件单次出现`，仅合并改动，作者归属分支原提交人）✅

### 4. 体验与内容基线（已落地）
- 像素风 UI v0.4 十四屏 1:1 真机 · 新手引导 `tutorial.py`（首局不再懵）· 委托与反制 UI（ardot 设计语言）
- 音效（8 个 CC0 合成 WAV；BGM 待补）· 地图配色对齐 S06 + 锚点信标 + 动效层 `ui_fx`
- 数据可视化 + 科技树描述重写 + v2 事件库扩充（37 + 6 条）

### 5. 工程化与验证
- 分享包自包含（`share/` 最新包 `dirty=no`，解包内跑三套验证通过）
- 回归三连全绿：`verify_tables` 38/38 · `verify_tech_tree` 29/29 · `test_build` 全过 · `test_v4_app` 59/59 · `test_edge_cases` 16/16 · `check_map_palette` 19/19
- 数值零回归：标准难度 47.3 周期 / 渗透 37.95% / 7 结局全在场（结论须 ≥200 局口径）

---

## 二、待办事项（按优先级）

### 🔴 P0 — 当前唯一真正阻塞
- **发出 M1 真人测试包 + 回收 ≥3 真人反馈**（当前 0 回收）。发包前 `python tools/repack.py` 重打并核对 `VERSION.txt` 指纹 = HEAD；随后补全《真人测试指南》机型 / 卡顿时段 / `crash.log` 回传三节

### 🟡 P1 — EA 前
- **前段压力倒挂**（原 P1-1）：立项新机制（前段"新奇效应"热度项 / 下调 `block_threshold`），别再调参；需 M1 反馈驱动
- **高压 10–22fps**：用 `--perf` 定位 `ui_v4_screens.py` 每 tick 重建 ~120 控件，预估 4–6 人时
- **easy 难度手感**：三轮标定留痕，待真人校准
- **大文件拆分**（pixel_assets / ui_v4_screens）：行数守卫已冻结，按 8 人时立项
- 数值均衡（被监管结局偏斜）、国家 vibe 差异化、build 多样性：依赖 M1 反馈

### 🟢 P2 — 打磨 / 1.0 前
- **P2-8 BGM / 演示视频 / 20 国介绍**：等素材来源（Q8）+ 真人反馈
- **怀疑度重标定专项**：旧标定 R1–R18 失效，须用归因修复后模拟器重跑（旧 30 局基线不可直接对比）
- **P1-2 AUTO 策略 + 方差回归**：90 局组间方差超标待修
- **真机目验**委托芯片条 / 反制 toast（M1 验收项）
- `verify_v4_shots` 基线进 pre-commit 可选档；UI 3 处小瑕疵（未解锁文案两义 / 空 lock 噪点 / HiDPI 技能带裁切）

---

## 快速开始

```bash
# 玩 Demo（Windows）
双击 demo/run_demo.bat
# Mac/Linux
cd demo && ./run_demo.sh
# 重打分享包（须对齐 HEAD：python tools/repack.py）
```

## 验证命令（改动后必跑）

```bash
python demo/verify_tables.py      # 38/38
python demo/verify_tech_tree.py   # 29/29
python demo/test_build.py         # 全过
python demo/test_v4_app.py        # 59/59
python demo/test_edge_cases.py    # 16/16
python demo/check_map_palette.py  # 19/19
python demo/balance_sim.py --seeds 30   # 与基线逐位一致（结论须 ≥200 局）
```

> 标准开发循环：小步改动 → 验证三连全绿 → 一个任务一个提交 → 立即推送。详见 CONTRIBUTING.md。

---

_最近更新：2026-09-12（R1/R2 整改闭环 + 试玩修复 + 采纳 V2 事件单次出现分支；删除无用提交 bc185d5、修正 #7 修复提交作者为 yehanyun168；分支采纳提交作者归属分支原人）_
