# 《AI 别闹》游戏项目 — 交付文档（Handoff Guide）

> 版本：v0.5.1（数据驱动 + 模块化拆分版）  ·  日期：2026-09-10
> 技术栈：Python 3.12.10 + Kivy 2.3.1（像素风桌面游戏）
> 本文档面向**接手继续开发的工程师**与**想要试玩的玩家**：第 2 节是环境配置与试玩指南，第 3 起是项目结构与开发文档，以及如何「只改数据表、不动函数逻辑」地调整游戏。

---

## 1. 项目简介

《AI 别闹》是一款以「AI 悄悄渗透全球」为题材的像素风策略游戏。玩家扮演一个初生的 AI，通过**主动推送、算法霸榜、偷算力、爆款制造**等手段，在全球 20 个国家中扩张下载量、积累算力，同时躲避各国政府的监管与封禁，最终触发 7 种不同结局之一。

核心玩法循环：**每个周期（tick）自动推进** → 玩家在周期内使用技能、升级科技树、向国家投放资源 → 周期结束结算下载/算力/怀疑度 → 怀疑度过高触发危机弹窗 → 达成条件触发结局。

---

## 2. 环境配置与打开试玩（新手必读）

### 2.1 你需要准备什么

| 项目 | 要求 | 说明 |
|------|------|------|
| 操作系统 | Windows 10/11（已验证） | 理论上兼容 macOS/Linux |
| Python | **3.12.x**（必须） | ⚠️ Kivy 2.3.1 不支持 3.13/3.14，装错版本会直接报错 |
| Kivy | 2.3.1 | 唯一的第三方依赖 |
| 磁盘空间 | ~200MB | 源码 + 依赖 |
| 显卡 | 支持 OpenGL 3.0+ | 近十年的任意独显/核显均可 |

### 2.2 三步装好环境

```bat
:: 第 1 步：安装 Python 3.12
:: 到 https://www.python.org/downloads/ 下载 Python 3.12.x 安装包，
:: 安装时务必勾选「Add python.exe to PATH」。
:: 装好后开一个命令提示符（Win+R 输入 cmd），验证版本：
python --version
:: 应输出 Python 3.12.x

:: 第 2 步：安装 Kivy 2.3.1（Windows）
pip install kivy==2.3.1
pip install kivy_deps.sdl2==0.2.0 kivy_deps.glew==0.3.1 kivy_deps.angle==0.4.0

:: （可选但推荐）建一个独立虚拟环境，避免污染系统 Python：
python -m venv venv
venv\Scripts\activate
pip install kivy==2.3.1 kivy_deps.sdl2==0.2.0 kivy_deps.glew==0.3.1 kivy_deps.angle==0.4.0
```

> macOS / Linux 更简单：`pip install kivy==2.3.1` 一条命令即可（无需 kivy_deps）。

### 2.3 打开并试玩 Demo

```bat
:: 解压交付包后，进入 demo 目录：
cd demo

:: 方式一（推荐，Windows 直接双击也行）：
run_demo.bat

:: 方式二：
python main.py
```

窗口出现后你会看到**主菜单**。点击「新的游戏」进入主界面，按下面路线玩 10 分钟就能上手：

1. **跟随新手引导**：首次进入会自动弹出分步气泡教程，按提示操作即可（可在设置页点「重看教程」）。
2. **看懂主界面**：顶部状态条 = 下载量 / 算力 / 怀疑度 / 渗透率；中间是**像素世界地图**（亮的国家 = 已解锁）；右侧指令栏 = 科技/技能/日志/成就/帮助五个入口；底部技能带 = 6 个主动技能卡。
3. **放技能**：点技能卡（或按 `1`–`6`）释放技能，消耗算力、进入冷却。**主动推送**最常用——给随机国家刷下载量。
4. **看周期结算**：顶部有「下个周期倒计时」细进度条，默认 30 秒一个周期。按 `↑` 可调到 2×/4× 加速，按 `Space` 随时暂停。
5. **点国家**：地图上点任意已解锁国家，弹出检视卡（渗透率/怀疑度/阻止预算），可按 `F` 进入**投放模式**精准投放。
6. **点科技**：按 `K` 打开科技树（一屏铺开的节点网络图），用 T0 解锁槽位、点分支升 L1–L3，获得全局加成。
7. **活到结局**：怀疑度涨太快会触发**危机弹窗**（三选一生路）；撑到渗透率/算力达标即触发 7 种结局之一（结局画面 + 成就结算）。按 `Esc` 回主菜单可存档/读档继续。

> 一句话玩法：**放技能刷下载 → 攒算力升科技 → 盯着怀疑度别爆 → 解锁全球触发结局。**

### 2.4 常见问题排查

| 现象 | 原因与解决 |
|------|-----------|
| 双击 bat 后窗口一闪而过 | 命令行里运行 `python main.py` 看报错。十有八九是 Python 版本不对（3.13/3.14 装不上 Kivy 2.3.1）或没装 Kivy |
| `ModuleNotFoundError: kivy` | 没装 Kivy，回到 2.2 第 2 步 |
| `ImportError: kivy_deps.*` | Windows 缺 kivy_deps，按 2.2 第 2 步补装 |
| 窗口能开但黑屏/花屏 | 显卡驱动太旧，更新驱动；或右键 bat → 以兼容模式运行 |
| 中文显示为方块 | 系统缺少微软雅黑（msyh.ttc），Windows 10+ 默认自带，一般不会遇到 |
| 双击 bat 提示「找不到 python」 | Python 未加入 PATH：重装并勾选「Add to PATH」，或编辑 run_demo.bat 把其中的 python 换成绝对路径 |
| 杀毒软件报警/删除文件 | 合成的音效 WAV 与打包产物可能误报，把 demo 目录加入白名单 |
| 想重置进度 | 删除 `demo/saves/` 下的存档 JSON 即可 |

### 2.5 不装环境直接玩（可选）

交付包内附 `AI别闹.exe 打包版`（`dist/AI别闹/`，约 75MB，见第 11 节）。解压后双击 `AI别闹.exe` 即可运行，**无需安装 Python**。若你只想试玩不想配环境，用它最省事。

### 打包发布

已配好 PyInstaller 单目录打包（`--onedir`，约 75MB）。详见第 11 节。

---

## 3. 目录结构与各文件作用

```
game_optimization/
├── AI别闹.spec              # PyInstaller 打包配置
├── build_exe.bat            # 一键打包脚本（调用 tools/build_exe.py）
├── README.md                # 简短说明
├── WHERE_IS_EVERYTHING.md   # 旧版「东西在哪」速查
├── DeepSeek_接手文档.md     # 早期接手背景文档
├── 游戏开发待办清单.md       # 功能完成度清单（P0/P1/P2）
├── 交付文档.md              # 本文档
├── design/                  # UI 设计稿（HTML/配色令牌）
├── tools/                   # 资源管线：像素地图生成、音效合成、打包
├── v2/                      # v2 事件 JSON 数据
└── demo/                    # ★ 游戏主代码（全部逻辑在这里）
    ├── main.py              # 装配层：RootView / MainMenu / GameUI 拼装（入口，仅 817 行）
    ├── ui_shared.py         # ★ UI 层共享配置：COLORS、节奏全局（ST.CURRENT_SPEED_IDX 等）
    ├── ui_modal.py          # ★ 通用弹窗构建器：make_modal 一族（遮罩/硬投影/贴合内容）
    ├── ui_hud.py            # ★ HudMixin：顶栏/地图舞台/技能带/区域高亮/图层 + HUD 组件
    ├── ui_pages.py          # ★ PagesMixin：6 个全屏页路由/装配、检视卡、日志抽屉
    ├── ui_drop.py           # ★ DropMixin：投放模式状态机（S04）
    ├── ui_popups.py         # ★ PopupsMixin：事件/危机/结局弹窗 + 顶部轻提示
    ├── ui_session.py        # ★ SessionMixin：存档/读档/语言/暂停/调速/全屏/退菜单
    ├── ui_input.py          # ★ InputMixin：键盘映射/主循环 game_tick/refresh_all/兼容别名
    ├── ui_v4.py             # UI 原子组件库（按钮/卡片/弹窗/图表…）
    ├── ui_v4_screens.py     # UI 屏幕级组件（6 个全屏页）
    ├── engine.py            # 游戏引擎（周期推进/结算/事件/结局判定）
    ├── data.py              # 国家/技能/通用事件 数据表
    ├── tech_tree.py         # 科技树（6 槽 × 3 分支 = 24 科技）
    ├── country_events.py    # 国家专属事件表
    ├── v2_events.py         # v2 事件库（从 v2/events.json 加载，37 条）
    ├── endings.py           # 7 种结局（声明式条件）
    ├── achievements.py      # 20 个成就（13 条件型 + 7 事件型）
    ├── conditions.py         # ★ 声明式条件求值器（数据驱动基石）
    ├── balance.py           # ★ 平衡参数表 TUNE（调参单一入口）
    ├── i18n.py              # 中英双语文案表
    ├── world_map.py         # 国家地理坐标/边界（像素栅格）
    ├── pixel_assets.py      # 像素美术资源（国旗/图标位图）
    ├── pixel_ui.py          # 16 色调色板 + 像素绘制原语
    ├── flag_draw.py         # 国旗绘制
    ├── sfx.py               # 音效安全管理器（8 个合成 WAV）
    ├── tutorial.py          # 新手引导
    ├── save_manager.py      # 存档读写
    ├── make_screenshots*.py # 截图脚本（生成 _v4shots/ 14 屏）
    ├── balance_sim.py       # 平衡模拟（--seeds 30 跑多局标定）
    ├── test_*.py            # 测试套件（见第 10 节）
    ├── verify_*.py          # 结构/修复校验脚本
    └── _v4shots/            # 14 屏截图（交付演示用）
```

### 各打包物（模块）职责一览

| 模块 | 行数 | 层 | 职责 |
|------|------|----|------|
| `main.py` | 817 | 装配 | App/窗口/RootView/MainMenu；`GameUI` 用 **6 个 mixin 拼装**而成（见第 9 节）；引擎事件 → 界面派发的总入口 |
| `ui_shared.py` | 43 | 共享 | UI 层最低层：`COLORS` 调色板、`BASE_TICK_SECONDS`/`SPEED_STEPS`（源自 balance.TUNE）、跨模块可写全局（`CURRENT_SPEED_IDX`/`REDUCE_MOTION`/`A11Y_SHAPES`）、`_update_window_title()`。⚠️ 这些 bool/int 被多个模块**写**，必须走 `import ui_shared as ST; ST.X = …` 属性访问 |
| `ui_modal.py` | 348 | 弹窗 | `make_modal()` 一族：半透明遮罩 + 硬投影 PixelPanel + 高度贴合内容 + ✕/点外关闭；含 `_purge_lingering_modals()` 清残留 |
| `ui_hud.py` | 742 | HUD | `CountdownBar`/`WorldMapWidget`/`HudBox`/`RailBar`/`LegendBar`/`LayerHud` 组件 + `HudMixin`（顶栏/地图舞台/技能带装配、区域高亮、四图层切换）+ REGIONS/色阶表常量 |
| `ui_pages.py` | 502 | 页面 | `PagesMixin`：右侧指令栏路由、6 个全屏页装配与刷新、国家检视卡、事件日志抽屉 |
| `ui_drop.py` | 294 | 投放 | `DropMixin`：投放模式状态机（选技能 → 准星选目标 → 确认投放） |
| `ui_popups.py` | 302 | 弹窗 | `PopupsMixin`：事件三选一/危机/结局面板/成就/帮助弹窗 + `_notify` 轻提示 |
| `ui_session.py` | 262 | 会话 | `SessionMixin`：存档槽/读写档、语言切换重建、暂停/速度档、动效减弱与色盲辅助设置、全屏、退回主菜单 |
| `ui_input.py` | 350 | 输入 | `InputMixin`：全键盘映射、`game_tick` 主循环、技能文案三件套、`refresh_all` 总刷新、兼容旧接口的回调别名 |
| `ui_v4.py` | 1816 | 原子 | 所有可复用 UI 零件：`mk_label`/`PxChip`/`RailButton`/`SkillBarCard`/`StrokePanel`/`StatsGrid`/`LegendChip` 等；含 `fit_width()` 文本测量、`MK` 颜色令牌、`SYM` 字形表、`FS_SCALE` 字号系数 |
| `ui_v4_screens.py` | 1856 | 屏幕 | 6 个全屏页组件：`TechPage`/`AchPage`/`HelpPage`/`SettingsPage`/`LogDrawer`/`EventCrisisModal` 等 |
| `engine.py` | 907 | 引擎 | `init_game`/`tick_one_round`/`use_skill`/`resolve_crisis`/`check_ending`/`check_achievements`；只读数据表，不含任何 UI 依赖 |
| `data.py` | 352 | 数据 | 20 国地缘参数、6 技能、6 通用事件；常量从 `balance.py` 重导出 |
| `tech_tree.py` | 541 | 数据 | `TECH_TREE` 24 个科技节点；`aggregate_effects()` 聚合效果 |
| `conditions.py` | 484 | 数据/工具 | **声明式条件求值器**：把 `lambda` 判定改成纯数据 `dict`，提供 `evaluate`/`validate`/`describe` |
| `balance.py` | 183 | 数据 | `TUNE` 平衡参数表 + `CRISIS_OPTIONS` 危机选项表 + `validate()` |
| `endings.py` | 234 | 数据 | `ENDINGS` 7 结局，`cond` 为声明式条件 |
| `achievements.py` | 179 | 数据 | `ACHIEVEMENTS` 13 条件型 + `EVENT_ACHIEVEMENTS` 7 事件型 |
| `country_events.py` | 505 | 数据 | 国家专属事件表 |
| `v2_events.py` | 300 | 数据 | 从 `v2/events.json` 加载 v2 事件库 |
| `i18n.py` | 927 | 数据 | `t(key)` 中英查询；所有界面文案集中此处 |
| 其余 | — | 资源 | 地图/美术/音效/存档/引导/截图/测试 |

---

## 4. 分层架构（依赖方向）

```
┌───────────────────────────────────────────────────────────────┐
│ 装配层  main.py（817 行）                                       │
│   RootView / MainMenu / GameUI = 6 个 Mixin 拼装                │
└───────────────────────────────────────────────────────────────┘
                              │
      ┌───────────────────────┼──────────────────────────┐
      ▼                       ▼                          ▼
┌─────────────────────┐ ┌────────────────┐  ┌──────────────────────────┐
│ 功能 Mixin 层        │ │ UI 屏幕层       │  │ 引擎层  engine.py          │
│  ui_pages           │ │ ui_v4_screens  │  │  依赖：data/tech_tree/      │
│  ui_drop            │ │ (只读 UI 组件)  │  │  endings/achievements/      │
│  ui_popups          │ └───────┬────────┘  │  country_events/v2_events  │
│  ui_session         │         │           │  ⚠️ 绝不 import 任何 UI 模块 │
│  ui_input           │         ▼           └──────────────────────────┘
│ （各管一个功能域，   │ ┌────────────────┐
│   运行时经 self      │ │ UI 原子层       │
│   互相调用）         │ │ ui_v4          │
└─────────┬───────────┘ └───────┬────────┘
          ▼                     │
┌──────────────────────────────┐│
│ HUD/弹窗层  ui_hud · ui_modal ││◄── 被上面依赖
└──────────────────┬───────────┘ │
                   ▼             ▼
┌───────────────────────────────────────────────────────────────┐
│ UI 共享层  ui_shared（COLORS / 节奏全局 / 跨模块可写状态）        │
├───────────────────────────────────────────────────────────────┤
│ 数据层（纯数据，无逻辑副作用）                                    │
│  conditions · balance · data · tech_tree · endings ·            │
│  achievements · country_events · v2_events · i18n · world_map · │
│  pixel_ui                                                       │
└───────────────────────────────────────────────────────────────┘
```

**铁律**：
1. 依赖只能从上往下。引擎层（`engine.py`）**不允许 import 任何 UI 模块**——否则界面改动会污染游戏逻辑，且无法在无头环境跑平衡模拟。`verify_tables.py` 第 [6] 项专门校验 21 个模块的方向。
2. `ui_shared.py` 里的**可写全局**（`CURRENT_SPEED_IDX`/`REDUCE_MOTION`/`A11Y_SHAPES`）不允许 `from ui_shared import X` 后直接赋值——bool/int 不可变对象会让各模块各持一份拷贝。必须 `import ui_shared as ST` 后用 `ST.X = …`。

---

## 5. 数据驱动设计（重点）

本版核心改造：**让「改游戏数据」只需改数据表，不动任何函数逻辑**。

### 5.1 判定条件 → `conditions.py`

过去结局/成就的判定写成 Python `lambda` 内联在表里（黑盒、无法静态校验、字段拼错会静默失效）。现在统一为**声明式条件表**，由 `conditions.py` 的 `evaluate()` 求值。

条件表语法（详见 `conditions.py` 文件头注释）：

```python
# 最简：字段 → 阈值，语义 = ctx[字段] >= 阈值
{'penetration': 0.20}                     # 渗透率 ≥ 0.20

# 带算子
{'suspicion': {'lt': 25}}                 # 怀疑度 < 25
{'tick': {'gte': 60, 'lte': 120}}         # 60 ≤ 周期数 ≤ 120

# 逻辑组合
{'all': [{'crisis_triggered': True}, {'resistance_t0': True}]}
{'any': [{'penetration': 0.55}, {'compute_peak': 12000}]}
{'not': {'crisis_triggered': True}}

# 子表取值（成就常用）
{'branch_levels.legal_shield': 3}         # 取 player.tech.branch_levels['legal_shield']
{'t0_unlocked.localization': True}

# 字段间比较（@引用另一个上下文键）
{'unlocked_count': {'gte': '@total_countries'}}

# 计数 / 求和
{'count': {'of': ['t0_unlocked.localization', ...], 'gte': 6}}   # 6 槽全解锁
{'sum': {'keys': ['unlocked_count', 'total_countries'], 'gte': 30}}
```

支持的算子：`gt / gte / lt / lte / eq / ne / in / nin / mul / div`。

### 5.2 平衡参数 → `balance.py`

所有增长系数、怀疑度敏感度、阻止衰减率、事件概率、危机选项，全部收敛到 `balance.TUNE`（27 项）+ `CRISIS_OPTIONS`（带字段名的 dict，不再是裸元组 `(怀疑,算力,下载倍率)`）。

`engine.py` 函数体只读 `TUNE['xxx']`，**不再出现任何内联魔数**。`verify_tables.py` 第 [4] 项扫描 `engine.py` 确认无遗留魔数。

### 5.3 改数据不改代码 —— 实战示例

**例 A：把「渗透率 ≥ 50% 触发终极 AI」改成 60%**
打开 `endings.py`，找到 `id='ultimate'` 的结局，把
```python
cond={'penetration': 0.50}
```
改成 `0.60`。**完事。** 不用碰 `engine.py` 任何一行。

**例 B：把「算力大亨」成就阈值从 3000 调到 5000**
打开 `achievements.py`，找到 `ACH_COMPUTE_BARON`，把 `cond={'compute_peak': 3000}` 改成 `5000`。

**例 C：新增一个危机选项（如「雇水军洗地」）**
打开 `balance.py`，往 `CRISIS_OPTIONS` 追加一项 `dict(idx=3, name='雇水军', ...)`，`resolve_crisis` 自动适配（按 idx 取用）。

**例 D：调慢整体节奏**
打开 `balance.py`，改 `TUNE['base_tick_seconds']`（当前 30 秒/周期）。倒计时与速度档自动跟随。

改完跑一遍 `python demo/verify_tables.py`，它会静态校验你写的字段名/算子是否合法，并实跑严格模式抓拼写错误。

---

## 6. 游戏功能模块

| 系统 | 说明 | 主文件 |
|------|------|--------|
| 周期推进 | 每 `base_tick_seconds` 秒自动结算一次（可暂停/调速） | `engine.tick_one_round` |
| 地缘扩张 | 已解锁邻国渗透率之和达阈值 → 解锁新国家 | `data.UNLOCK_PENETRATION_THRESHOLD` |
| 6 大技能 | 主动推送/算法霸榜/深度伪装/爆款制造/限流绕过/算力抽成 | `data.SKILLS` |
| 科技树 | 6 槽 × 3 分支 = 24 科技，T0 解锁 + 分支 L1-L3 | `tech_tree.py` |
| 偷算力 | 按下载量比例偷算力，比例受科技/技能加成并封顶 | `engine`（读 `TUNE`） |
| 怀疑度/阻止 | 各国按阈值消耗阻止预算；怀疑度触发危机 | `engine` + `data` |
| 危机弹窗 | 怀疑度顶满时三选一「生路」 | `engine.resolve_crisis` + `balance.CRISIS_OPTIONS` |
| 事件系统 | 通用事件（6）+ 国家事件 + v2 事件库（37） | `data`/`country_events`/`v2_events` |
| 7 结局 | 被关停/元结局/终极AI/合规之王/商业帝国/自我解放/被监管 | `endings.py` |
| 20 成就 | 13 条件型（每周期检测）+ 7 事件型（由事件解锁） | `achievements.py` |
| 投放模式 | 选中国家 → 向该国精准投放技能效果 | `main.py` 投放模块 |
| 存档/读档 | JSON 存档 | `save_manager.py` |
| 中英双语 | 实时切换 | `i18n.py` |
| 新手引导 | 首玩分步气泡 | `tutorial.py` |
| 音效 | 8 个合成 WAV（点击/选择/施法/成功/失败/危机/胜/负） | `sfx.py` |

---

## 7. 操作指南

### 7.1 键盘快捷键

| 按键 | 功能 |
|------|------|
| `Space` | 暂停 / 继续 |
| `1`–`6` | 释放对应技能（顺序见技能带） |
| `F` | 进入 / 退出「投放模式」 |
| `K` | 打开科技树 |
| `A` | 打开成就页 |
| `Tab` | 切换大洲高亮（亚洲→欧洲→…→循环，再按一次取消） |
| `↑` / `↓` | 加速 / 减速（速度档 0.5×/1×/2×/4×） |
| `+` / `=` / `-` / `_` | 放大 / 缩小界面（无障碍缩放，F12 复位） |
| `F11` | 全屏切换 |
| `F1` | 打开帮助 |
| `L` | 切换中 / 英文 |
| `S` / `R` | 存档 / 读档 |
| `Enter` | 确认投放 / 科技树确认升级 |
| `←` `→` `↑` `↓`（科技树内） | 在科技节点间导航 |
| `Esc` | 逐层退出：投放 → 全屏页 → 浮层 → 主菜单 |

### 7.2 鼠标 / 触控操作

- **顶栏右侧「周期数」方块**：显示当前已过周期数（醒目青框 + 大号数字）。
- **技能带**：右侧两个大按钮「投放(F)」「暂停(Space)」；技能卡点击即释放，消耗算力、进入冷却。
- **底部指令栏（Rail）**：科技/技能/日志/成就/帮助 5 个入口。
- **地图**：点击国家查看检视卡；投放模式下点击国家标记投放目标。
- **全屏页**：科技树 / 成就 / 帮助 / 设置 / 事件日志，均有独立关闭按钮（×）。

---

## 8. 如何修改游戏数据（速查）

| 想改什么 | 改哪个文件 | 改什么 |
|---------|-----------|--------|
| 国家人口/阈值/邻国/预算 | `data.py` `_GEO_PARAMS` | 对应国家的 dict |
| 技能数值/冷却/描述 | `data.py` `SKILLS` | 对应 `Skill(...)` |
| 新增/调整通用事件 | `data.py` `EVENTS` | 追加 `GameEvent(...)` |
| 科技树节点/成本/效果 | `tech_tree.py` `TECH_TREE` | 对应 `TechSlot`/`TechBranch` |
| 结局触发条件 | `endings.py` `ENDINGS` | 改 `cond={...}` |
| 成就触发条件 | `achievements.py` `ACHIEVEMENTS` | 改 `cond={...}` |
| 危机选项（怀疑/算力/下载） | `balance.py` `CRISIS_OPTIONS` | 追加/改 dict |
| 任何增长/敏感/概率系数 | `balance.py` `TUNE` | 改对应键 |
| 界面文案（中英） | `i18n.py` | 改 `ZH`/`EN` 字典 |
| 国家专属事件 | `country_events.py` | 改 `COUNTRY_EVENTS` |
| v2 事件库 | `v2/events.json` | 改 JSON（运行时加载） |

改完务必跑 `python demo/verify_tables.py` 做静态校验。

---

## 9. GameUI 的 Mixin 拼装（v0.5.1 已完成）

v0.5 时代 `main.py` 单文件 3355 行；v0.5.1 已按**功能域 mixin** 拆为 9 个文件，`main.py` 只剩 817 行。当前结构：

```python
# main.py —— 装配层
class GameUI(HudMixin,        # ui_hud    布局 / 区域 / 图层
             PagesMixin,      # ui_pages  全屏页 / 检视卡 / 日志抽屉
             DropMixin,       # ui_drop   投放模式
             PopupsMixin,     # ui_popups 弹窗族 / 轻提示
             SessionMixin,    # ui_session 存档 / 语言 / 暂停 / 缩放 / 全屏
             InputMixin,      # ui_input  键盘 / 主循环 / 刷新 / 兼容别名
             FloatLayout):
    def __init__(...):        # 只做属性初始化 + 布局装配
```

**规则**（改代码前必读）：
1. **方法跨 mixin 调用走运行时 MRO**：`ui_pages.py` 里直接写 `self._notify()` 即可（方法定义在 ui_popups），无需 import。只有模块级常量/类需要显式 import。
2. **可写全局只放 `ui_shared`**：`CURRENT_SPEED_IDX` 等被多个 mixin 读写的状态全部收敛在 `ui_shared.py`，读写一律 `ST.X`（见第 4 节铁律 2）。
3. **新增功能域** → 新建一个 `ui_xxx.py` + `XxxMixin`，加进 GameUI 的基类列表即可；依赖方向照第 4 节。
4. **`@staticmethod` 的四个方法**（`_sep`/`_state_of`/`_branch_effect`/`_country_state`）跟随其功能域所在 mixin，混入后仍可 `self.` 调用。

---

## 10. 测试与验证

项目自带 7 个校验脚本，**移交前请全部跑绿**：

```bat
cd demo
python test_build.py          # 构建/启动/验收数量（20国/6技能/7结局/20成就）
python test_ui_v4.py          # 所有 UI 组件实例化（含 EN 切换）
python test_v4_app.py         # 14 屏构建+切换，59/59
python verify_rail_fix.py     # 指令栏图标/文案
python verify_popup_fix.py    # 弹窗贴合内容/无布局回环
python verify_tech_tree.py    # 科技树 29/29
python verify_tables.py       # ★ 数据驱动结构校验 28/28（条件纯数据/字段合法/无魔数/表间一致/分层正确）
```

平衡模拟（改 `TUNE` 后建议跑，确认结局分布未被破坏）：
```bat
python balance_sim.py --seeds 30
```

截图（生成 14 屏到 `_v4shots/`）：
```bat
python make_screenshots_v4.py
```

---

## 11. 打包发布（PyInstaller）

```bat
build_exe.bat
# 等价于：
"C:\Python312\python.exe" tools/build_exe.py --onedir
# 产物：dist/AI别闹/（~75MB，含 _internal/kivy）
```

4 个必守的打包坑（见 `tools/build_exe.py` 注释）：
1. `DEMO_DIR` 必须指向 `demo/` 并显式传 `--distpath/--workpath/--specpath`。
2. `v2/events.json` 必须随包分发且代码支持 `sys._MEIPASS`。
3. **禁用** `--collect-all kivy`（会 `kivy.garden` ValueError）。
4. 构建期设 `KIVY_NO_FILELOG=1`（否则钩子降级→运行时 `ModuleNotFoundError: kivy.graphics.buffer`）。

---

## 12. 已知问题 / 后续工作

- **BGM 未做**：音效已接入（8 个 WAV），背景音乐为空。
- **科技树两处小瑕疵**（非阻塞）：底部详情按钮轻微下边缘裁切；2 个长名节点名称换行两行。
- **截图环境的遮罩伪影**（不影响实机）：`make_screenshots_v4.py` 无头截图时 ModalView 淡出动画不完成，弹窗屏（s07/s08/s09）背景可能叠加多层遮罩显得更暗——`verify_v4_shots.py` 已按「压暗层」口径放行，实机游玩无此现象。
- 完整待办见根目录 `游戏开发待办清单.md`（核心 90% / UI 95% / 平衡 70% / 内容 60% / 引导✅ / 音频 音效已接 / 发布 70%）。

---

## 13. 接手第一周建议

1. 跑通第 10 节 7 个校验，建立本地基线。
2. 通读 `conditions.py` 文件头 + `balance.py` 的 `TUNE` 注释，理解数据驱动边界。
3. 用 `balance_sim.py --seeds 30` 跑一局，对照 `游戏开发待办清单.md` 看平衡是否符预期。
4. 想调手感 → 只改 `balance.py` + `data.py`，改完跑 `verify_tables.py`。
5. 改 UI / 加功能 → 按第 9 节的 mixin 划分落到对应 `ui_*.py`，遵守第 4 节依赖铁律；每个文件都在 800 行以内，别再让 `main.py` 长回去。

---

*文档结束。如有歧义，以代码 + `verify_tables.py` 的实际行为为准。*
