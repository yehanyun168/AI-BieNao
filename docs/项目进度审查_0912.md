# 《AI 别闹》项目进度审查 — 2026-09-12

| 项 | 内容 |
|---|---|
| 审查时间 | 2026-09-12 13:07 |
| 代码基线 | `bb3d3b8`（工作区干净，HEAD == 最新提交 13:00:54） |
| 对照文档 | `docs/游戏项目改进优化计划书.md`（v1.0，09-12 00:37 五角色评审） |
| 验证执行 | L1 四件套 + L2 30 种子模拟，本机实跑（见 §3） |
| 审查结论 | **代码侧 ON TRACK 且超前——R2/R3 大半已提前完成；真正的瓶颈只有一个：真人测试（M1）零回收，且现行分享包已再次落后 HEAD** |

---

## 1. 一页总览

**一句话：26 项整改里 P0 全清、P1 完成 8/11，计划发布后约 12 小时干完了原定 3 轮的大部分活；但"发给真人玩"这件事从昨天到现在仍然是 0 进度——代码越超前，包越旧，M1 越空转。**

| 分层 | 计划 | 已完成 | 状态 |
|---|---|---|---|
| P0（阻塞发包） | 7 | **7** | ✅ 全清（`04ab6bc` + `beb551d`，09-12 02:43） |
| P1（重要优化） | 11 | **8** | ◐ 剩 P1-1 / P1-7 / P1-8（其中 P1-1 按计划等真人反馈） |
| P2（长期债） | 8 | 0 | ⬜ 未开始（符合计划，排 R4 / 1.0 前） |

## 2. P1 逐项核对（8 ✅ / 3 ⬜）

| # | 内容 | 状态 | 证据（commit / 文件） |
|---|---|---|---|
| P1-2 | 技能效果预览 `preview_skill` | ✅ | `14082ee`（10:54） |
| P1-3 | 帧率探针 `--perf` | ✅ | `14082ee`；`demo/perf.log` 已有实测：空闲 46–48 fps、p50≈20.5ms、p95≈22ms、长帧 0 |
| P1-4 | 合规结局专精策略验证 | ✅ | `84bc31f`；`balance_sim.py --strategy compliance` 人格已可用 |
| P1-5 | 色觉 ΔE + 对比度达标 | ✅ | `84bc31f` |
| P1-6 | 视觉基线重建 + 三处目验 | ✅ | `de1d3c4`；顺带修掉 HudBox 子级画在 (0,0) 的存量渲染缺陷；`_v4shots/` 已刷新（09-12 12:08） |
| P1-9 | 存档原子写 + 迁移骨架 | ✅ | `bb3d3b8`（13:00） |
| P1-10 | TUNE 快照治理（运行时可调） | ✅ | `bb3d3b8`；L2 复跑与基线逐位一致，证明"值相同零漂移"达成 |
| P1-11 | toast 避让顶栏 + 宽度封顶 | ✅ | `cb1474e`（12:27） |
| P1-1 | 前段压力倒挂改造（6h） | ⬜ 按计划等反馈 | 出口指标未动：首危机 43.2 周期（目标 ≤30）、12/30 局无危机（目标 ≤3）、单周期峰值解锁 6 国（目标 ≤3） |
| P1-7 | 引擎 8 项边界用例 | ⬜ 半成品 | `test_edge_cases.py` 已建但只含 P0-2 的 4 条存档容错用例，文件头注明"P1-7 的边界用例也往这里加" |
| P1-8 | 分层护栏扩容 22→43 模块 | ⬜ | `verify_tables.py` LAYERS 仍为 22 个模块，17 个运行时模块未纳管 |

## 3. 本机实跑验证（当前 HEAD）

**L1 四件套 — 全绿 ✅**
- `verify_tables.py` → **35/35**
- `verify_tech_tree.py` → **29/29**
- `test_build.py` → 17 节全部通过
- `test_v4_app.py` → 14 屏全部可构建（59/59）

**L2 数值模拟 `balance_sim --seeds 30` — 与评审基线一致 ✅**

| 指标 | 本轮实测 | 评审基线（09-12 凌晨） | 判定 |
|---|---|---|---|
| 平均局长 | 47.3 周期（38–55） | 47.3 | 一致 |
| 平均渗透 | 37.95% | 37.95% | 逐位一致 |
| 平均解锁 | 18.7 / 20 国 | 18.9 | ±0.2 正常波动 |
| 结局分布 | 7 结局全在场 | 同 | ✅ |

结局分布：自我解放 26.7% / 终极 AI 23.3% / 商业帝国 16.7% / 被关停 16.7% / 被监管 6.7% / 元结局 6.7% / **合规之王 3.3%**（已知：默认 AUTO 策略局限，Q1 已拍板"只加策略验证不动阈值"，属预期值）。

> 结论：今天 8 个提交（含 data.py/engine.py 重构）**零数值回归**。

## 4. 路线图对位（计划书 §5）

| 轮 | 原定节奏 | 实际 |
|---|---|---|
| **R1** 让包可安全发出（8.8h） | 第一轮 | ✅ **02:43 完成**（P0-1~7 + repack 指纹修复），发包验收 A1–A8 的代码侧前提全部就位 |
| **R2** 按反馈修体验（17.5h） | 等反馈后 | ◐ **提前完成 6/7**（P1-2/3/4/5/6/11）；P1-1 特意留待反馈（正确——它需要真人数据驱动） |
| **R3** 数据可调+边界有网（18.5h） | 第三轮 | ◐ **2/3 提前完成**（P1-9 存档原子写、P1-10 TUNE 治理）；P1-7 边界用例未动 |
| **R4** EA 门清债（15h） | 第四轮 | ⬜ 未开始（800 行守卫 / CI / exe 等，符合计划） |

**速度评价**：单人约 12 小时完成了原估算 ≈27 人时的 R2+R3 主体，且每项都有独立 commit、L1/L2 全绿——执行质量与计划一致。

## 5. 缺口与风险（本次审查新发现）

| # | 发现 | 严重度 | 说明 |
|---|---|---|---|
| G-1 | **M1 真人测试 0 回收** | 🔴 最高 | 从 09-11"代码侧就绪"到现在，"≥3 真人跑一局反馈"始终是 0。执行纪律第 3 条：任何 P0/P1 完成都需真人反馈才能判 done。当前 8 项 P1 的"✅"严格说都是"代码完成"，不是"验证完成" |
| G-2 | **分享包再次落后 HEAD** | 🔴 高 | 最新包 `AI_Bienao_v2_20260912_0247.zip` 指纹 = `beb551d`（02:47），HEAD 已到 `bb3d3b8`（13:00），**落后 6 提交**，缺 P1-2 技能预览、P1-5 配色、P1-9 存档原子写、P1-10 TUNE、P1-11 toast 修复。正是计划书 R-3 风险的复现——发包前必须 `python tools/repack.py` 重打并核对指纹 |
| G-3 | 真人测试指南未按 R1 要求更新 | 🟡 中 | `docs/真人测试指南.md` 停在 09-11 19:54，缺计划书 R1 交付物要求的"机型 / 卡顿时段 / crash.log 回传"三节。P1-3 探针已有数据，指南补上后可让测试者直接回报性能 |
| G-4 | zip 包内中文路径命令行解压显示乱码 | 🟢 低 | unzip 下文件名乱码（GBK vs UTF-8），Windows 资源管理器通常正常；建议按 §6.3 做一次中文路径实机解压验证 |
| G-5 | 帧率基线偏差 | 🟢 低 | perf 实测空闲 46–48 fps，低于建议基线 55 fps（计划书注明该基线"未经真机实测待校准"，Q4 未拍板）。p95 22ms、长帧 0，体感应无问题，建议在 Q4 拍板时把 46–48 定为作者机基线 |

## 6. 下一步建议（严格排序）

1. **重打分享包**（`python tools/repack.py`）→ 指纹核对 = HEAD → **更新真人测试指南**（补机型/卡顿/crash 回传三节）→ **发给 3 位真人**。这是唯一真正阻塞项目的事，其余一切都在等它。
2. 等反馈期间可做（≈6h，互不阻塞）：P1-7 边界用例补齐（往 `test_edge_cases.py` 追加 8 条）+ P1-8 分层护栏扩容（LAYERS 22→33+）。
3. P1-1 前段压力改造：**等 M1 反馈回来再做**（它的三项改动都需要真人数据支撑，提前做是浪费）。
4. M1 反馈回收后：按计划书 §5.3 出口 B1–B7 验收，P1-1 落地，进 R3 收尾。

---

_审查方法：只读核查 git 历史（30 commits）+ 计划书逐项比对 + 本机实跑 L1/L2 验证 + 分享包指纹解包比对。未修改任何项目文件。_
